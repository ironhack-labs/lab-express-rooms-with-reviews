<h1>List of all rooms</h1>

{{#each rooms}}

<div class="">

<h2>Name: {{name}}</h2>

<p>Description: {{description}}</p>

<img class="" src="{{imageUrl}}" alt="{{name}}" />

<h4>Owner: {{owner.name}}</h4>
<h4>Owner: {{owner.email}}</h4>

<a href="/rooms/{{_id}}">See more</a>
</div>
  {{else}}
    <p>There are no rooms yet!</p>

{{/each}}